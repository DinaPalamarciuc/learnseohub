<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1.0"/>
<title>AI SEO Score Card — LearnSEO Hub</title>
<meta name="description" content="Instant AI-powered SEO analysis for any page — visual score card with radar chart, E-E-A-T audit, GEO readiness, and PDF export. Free."/>
<link rel="canonical" href="https://learnseohub.com/tools/seo-scorecard"/>
<link rel="stylesheet" href="../css/style.css"/>
<!-- jsPDF for real PDF export -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<style>
/* ═══════════════════════════════════════
   SEO SCORE CARD — Design System
   Aesthetic: Dark Precision + Neon Glow
═══════════════════════════════════════ */

/* Page-level dark atmosphere */
html { scroll-behavior: smooth; }

.sc-page-bg {
  position: fixed; inset: 0;
  background:
    radial-gradient(ellipse 800px 600px at 8% 12%,  rgba(0,102,255,.09) 0%, transparent 55%),
    radial-gradient(ellipse 600px 700px at 92% 88%, rgba(139,92,246,.07) 0%, transparent 55%),
    radial-gradient(ellipse 400px 400px at 50% 40%, rgba(0,184,124,.04) 0%, transparent 65%);
  pointer-events: none; z-index: 0;
  animation: scBgPulse 18s ease-in-out infinite alternate;
}
@keyframes scBgPulse {
  0%   { opacity:.6; }
  100% { opacity:1; }
}

/* ── HERO ── */
.sc-hero {
  text-align: center;
  padding: 52px 0 44px;
  position: relative; z-index: 1;
}
.sc-eyebrow {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 6px 18px; margin-bottom: 22px;
  border-radius: 30px;
  border: 1px solid rgba(0,102,255,.22);
  background: rgba(0,102,255,.06);
  font-size: 11px; font-weight: 700;
  color: var(--accent);
  text-transform: uppercase; letter-spacing: .1em;
}
.sc-eyebrow::before {
  content: '';
  width: 6px; height: 6px; border-radius: 50%;
  background: var(--accent);
  box-shadow: 0 0 10px var(--accent);
  animation: eyebrowBlink 2.2s ease-in-out infinite;
}
@keyframes eyebrowBlink { 0%,100%{opacity:1;} 50%{opacity:.25;} }

.sc-hero h1 {
  font-size: clamp(28px,5vw,52px);
  font-weight: 900; letter-spacing: -.035em;
  line-height: 1.1; margin-bottom: 16px;
  color: var(--t1);
}
.sc-hero h1 .grad {
  background: linear-gradient(135deg, var(--accent), var(--vi));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.sc-hero p {
  font-size: 15px; color: var(--t2);
  line-height: 1.7; max-width: 560px;
  margin: 0 auto 40px;
}

/* ── URL FORM ── */
.url-form-wrap { max-width: 680px; margin: 0 auto 12px; }
.url-form {
  display: flex; gap: 0;
  border: 1px solid var(--border);
  border-radius: 16px; overflow: hidden;
  background: var(--card);
  backdrop-filter: blur(16px);
  transition: all .3s ease;
  box-shadow: 0 4px 24px rgba(0,0,0,.06);
}
.url-form:focus-within {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(0,102,255,.1), 0 8px 30px rgba(0,0,0,.08);
}
.url-inp {
  flex: 1; padding: 17px 20px;
  background: transparent; border: none; outline: none;
  font-size: 14px; color: var(--t1);
  font-family: 'JetBrains Mono', monospace;
}
.url-inp::placeholder { color: var(--t3); font-family: inherit; }
.url-go {
  padding: 14px 26px;
  background: linear-gradient(135deg, var(--accent), var(--vi));
  border: none; color: #fff;
  font-size: 14px; font-weight: 700;
  cursor: pointer; font-family: inherit;
  display: flex; align-items: center; gap: 8px;
  transition: all .3s ease; white-space: nowrap;
}
.url-go:hover { filter: brightness(1.12); }
.url-go:disabled { opacity: .5; cursor: not-allowed; }

.prov-row {
  display: flex; align-items: center; justify-content: center;
  gap: 10px; flex-wrap: wrap; margin-top: 14px;
}
.prov-chip {
  display: flex; align-items: center; gap: 6px;
  padding: 6px 14px; border-radius: 8px;
  border: 1px solid var(--border);
  background: transparent; color: var(--t3);
  font-size: 12px; font-weight: 600;
  cursor: pointer; font-family: inherit;
  transition: all .2s;
}
.prov-chip:hover, .prov-chip.active {
  border-color: var(--accent); color: var(--accent);
  background: rgba(0,102,255,.06);
}
.prov-chip .dot { width: 6px; height: 6px; border-radius: 50%; background: currentColor; }
.key-toggle {
  font-size: 12px; color: var(--accent); background: none; border: none;
  cursor: pointer; text-decoration: underline; font-family: inherit;
}

/* ── API KEY DRAWER ── */
.key-drawer {
  display: none; max-width: 680px; margin: 12px auto 0;
  padding: 18px 20px;
  background: rgba(0,102,255,.04);
  border: 1px solid rgba(0,102,255,.14);
  border-radius: 14px;
}
.key-drawer.open { display: block; }
.key-row { display: flex; gap: 10px; margin-top: 10px; }
.key-inp {
  flex: 1; padding: 11px 14px;
  background: var(--inputBg); border: 1px solid var(--border);
  border-radius: 10px; color: var(--t1);
  font-family: 'JetBrains Mono', monospace; font-size: 13px;
  outline: none; transition: border-color .2s;
}
.key-inp:focus { border-color: var(--accent); }
.key-save {
  padding: 11px 20px; border-radius: 10px;
  background: rgba(0,102,255,.1); border: 1px solid rgba(0,102,255,.25);
  color: var(--accent); font-size: 13px; font-weight: 700;
  cursor: pointer; font-family: inherit; transition: all .2s; white-space: nowrap;
}
.key-save:hover { background: rgba(0,102,255,.18); }
.key-saved { display: none; font-size: 12px; color: var(--em); margin-top: 6px; }

.err-banner {
  display: none; max-width: 680px; margin: 12px auto 0;
  padding: 12px 18px;
  background: rgba(239,68,68,.07); border: 1px solid rgba(239,68,68,.2);
  border-radius: 12px; color: #dc2626; font-size: 13px;
}
[data-theme="dark"] .err-banner { color: #fca5a5; background: rgba(239,68,68,.12); }

/* ── LOADING ── */
.sc-loading {
  display: none; text-align: center;
  padding: 80px 24px; position: relative; z-index: 1;
}
.tri-ring {
  width: 72px; height: 72px;
  margin: 0 auto 28px; position: relative;
}
.tri-ring::before, .tri-ring::after {
  content: ''; position: absolute; border-radius: 50%;
  border: 3px solid transparent; animation: triSpin 1.3s linear infinite;
}
.tri-ring::before {
  inset: 0; border-top-color: var(--accent); border-right-color: var(--accent); animation-duration: 1s;
}
.tri-ring::after {
  inset: 12px; border-bottom-color: var(--vi); border-left-color: var(--vi);
  animation-direction: reverse; animation-duration: .72s;
}
@keyframes triSpin { to { transform:rotate(360deg); } }
.sc-steps {
  display: flex; flex-direction: column; gap: 8px;
  max-width: 320px; margin: 24px auto 0;
}
.sc-step {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 16px; border-radius: 10px;
  font-size: 13px; color: var(--t3);
  background: var(--bg2); border: 1px solid var(--border);
  transition: all .5s ease;
}
.sc-step.on  { color: var(--accent); background: rgba(0,102,255,.06); border-color: rgba(0,102,255,.2); }
.sc-step.done{ color: var(--em);    background: rgba(0,184,124,.05); border-color: rgba(0,184,124,.15); }

/* ── RESULTS ── */
.sc-results { display: none; position: relative; z-index: 1; }

.results-topbar {
  display: flex; align-items: center; justify-content: space-between;
  flex-wrap: wrap; gap: 12px; margin-bottom: 24px;
  padding: 18px 22px;
  background: rgba(0,102,255,.05);
  border: 1px solid rgba(0,102,255,.12);
  border-radius: 16px;
}
.results-url { font-size: 12px; color: var(--t3); word-break: break-all; }
.results-title { font-size: 18px; font-weight: 800; color: var(--t1); margin-top: 3px; }
.action-btns { display: flex; gap: 8px; flex-wrap: wrap; }
.btn-pdf {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 11px 22px; border-radius: 12px;
  background: linear-gradient(135deg, var(--accent), var(--vi));
  color: #fff; border: none;
  font-size: 13px; font-weight: 700;
  cursor: pointer; font-family: inherit;
  box-shadow: 0 6px 22px rgba(0,102,255,.25);
  transition: all .3s cubic-bezier(.34,1.56,.64,1);
}
.btn-pdf:hover { transform: translateY(-2px); box-shadow: 0 10px 32px rgba(0,102,255,.35); }
.btn-sec {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 10px 18px; border-radius: 12px;
  border: 1px solid var(--border); background: var(--bg2);
  color: var(--t2); font-size: 13px; font-weight: 600;
  cursor: pointer; font-family: inherit;
  transition: all .2s;
}
.btn-sec:hover { border-color: var(--accent); color: var(--accent); }

/* ── MAIN GRID ── */
.main-grid {
  display: grid;
  grid-template-columns: 320px 1fr;
  gap: 20px; margin-bottom: 20px;
}
@media(max-width:860px){ .main-grid { grid-template-columns: 1fr; } }

/* ── SCORE CARD ── */
.score-panel {
  background: var(--card);
  backdrop-filter: blur(20px);
  border: 1px solid var(--border);
  border-radius: 22px; padding: 30px 24px;
  text-align: center; position: relative; overflow: hidden;
}
.score-panel::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0; height: 3px;
  background: linear-gradient(90deg, var(--accent), var(--vi), var(--em));
}
.score-ring-wrap {
  position: relative; width: 154px; height: 154px;
  margin: 0 auto 18px;
}
.score-ring-wrap svg { width: 100%; height: 100%; transform: rotate(-90deg); }
.ring-bg  { fill:none; stroke: var(--border); stroke-width: 9; }
.ring-val {
  fill: none; stroke-width: 9; stroke-linecap: round;
  stroke-dasharray: 440; stroke-dashoffset: 440;
  transition: stroke-dashoffset 1.6s cubic-bezier(.34,1.56,.64,1);
}
.ring-center {
  position: absolute; inset: 0;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
}
.score-big  { font-size: 46px; font-weight: 900; color: var(--t1); line-height: 1; }
.score-outof{ font-size: 12px; color: var(--t3); font-weight: 600; margin-top: 2px; }

.score-grade  { font-size: 20px; font-weight: 900; color: var(--t1); margin-bottom: 5px; }
.score-tagline{ font-size: 13px; color: var(--t2); line-height: 1.5; margin-bottom: 22px; }

.dim-row {
  display: flex; align-items: center; gap: 8px;
  margin-bottom: 9px;
}
.dim-name { font-size: 11px; color: var(--t3); width: 88px; text-align: left; flex-shrink: 0; }
.dim-bar-bg {
  flex: 1; height: 5px; border-radius: 3px;
  background: var(--bg2); overflow: hidden;
}
.dim-bar-fill {
  height: 100%; border-radius: 3px;
  background: linear-gradient(90deg, var(--accent), var(--vi));
  transition: width 1.3s cubic-bezier(.34,1.56,.64,1);
}
.dim-val { font-size: 11px; font-weight: 700; color: var(--t2); width: 26px; text-align: right; flex-shrink: 0; }

/* ── RADAR PANEL ── */
.radar-panel {
  background: var(--card);
  backdrop-filter: blur(20px);
  border: 1px solid var(--border);
  border-radius: 22px; padding: 26px;
}
.radar-panel h4 {
  font-size: 14px; font-weight: 700; color: var(--t1);
  margin-bottom: 16px; text-align: center;
}
#radarCanvas { display: block; max-width: 100%; margin: 0 auto; }

/* ── METRIC CARDS ── */
.metric-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
  gap: 14px; margin-bottom: 20px;
}
.mc {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 16px; padding: 20px 18px;
  position: relative; overflow: hidden;
  transition: all .3s ease;
}
.mc:hover { transform: translateY(-3px); border-color: var(--accent); box-shadow: 0 12px 30px rgba(0,102,255,.08); }
.mc-accent-bar {
  position: absolute; top: 0; left: 0; right: 0; height: 3px;
  border-radius: 16px 16px 0 0;
}
.mc-icon { font-size: 26px; margin-bottom: 10px; }
.mc-name { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing:.07em; color: var(--t3); margin-bottom: 5px; }
.mc-val  { font-size: 30px; font-weight: 900; color: var(--t1); line-height: 1; margin-bottom: 6px; }
.mc-sub  { font-size: 11px; color: var(--t3); margin-bottom: 10px; }
.mc-bar  {
  height: 4px; border-radius: 2px;
  background: var(--bg2); overflow: hidden;
}
.mc-bar-fill {
  height: 100%; border-radius: 2px;
  transition: width 1.3s cubic-bezier(.34,1.56,.64,1);
}

/* ── ISSUES / WINS ── */
.two-col {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 18px; margin-bottom: 20px;
}
@media(max-width:640px){ .two-col { grid-template-columns: 1fr; } }
.iw-panel {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 18px; padding: 22px;
}
.iw-header {
  display: flex; align-items: center; gap: 10px;
  margin-bottom: 16px;
}
.iw-icon {
  width: 32px; height: 32px; border-radius: 9px;
  display: flex; align-items: center; justify-content: center;
  font-size: 15px; flex-shrink: 0;
}
.iw-title { font-size: 14px; font-weight: 700; color: var(--t1); }

.issue-item, .win-item {
  display: flex; gap: 10px; align-items: flex-start;
  padding: 10px 0; border-bottom: 1px solid var(--border);
  font-size: 13px; color: var(--t2); line-height: 1.55;
}
.issue-item:last-child, .win-item:last-child { border-bottom: none; }
.issue-dot {
  width: 8px; height: 8px; border-radius: 50%;
  flex-shrink: 0; margin-top: 4px;
}
.issue-dot.high { background: #ef4444; box-shadow: 0 0 7px rgba(239,68,68,.6); }
.issue-dot.med  { background: #f59e0b; box-shadow: 0 0 7px rgba(245,158,11,.5); }
.issue-dot.low  { background: var(--accent); box-shadow: 0 0 7px rgba(0,102,255,.5); }
.win-dot { width:8px; height:8px; border-radius:50%; flex-shrink:0; margin-top:4px; background: var(--em); box-shadow: 0 0 7px rgba(0,184,124,.5); }

/* ── RECOMMENDATIONS ── */
.recs-panel {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 18px; padding: 26px; margin-bottom: 20px;
}
.rec-item {
  display: flex; gap: 16px;
  padding: 16px 0; border-bottom: 1px solid var(--border);
}
.rec-item:last-child { border-bottom: none; padding-bottom: 0; }
.rec-num {
  width: 36px; height: 36px; border-radius: 10px;
  background: rgba(0,102,255,.08);
  border: 1px solid rgba(0,102,255,.18);
  display: flex; align-items: center; justify-content: center;
  font-size: 14px; font-weight: 800; color: var(--accent);
  flex-shrink: 0;
}
.rec-body { flex: 1; }
.rec-title { font-size: 14px; font-weight: 700; color: var(--t1); margin-bottom: 5px; }
.rec-desc  { font-size: 13px; color: var(--t2); line-height: 1.6; }
.effort-badge {
  display: inline-flex; align-items: center; gap: 4px;
  padding: 2px 9px; border-radius: 6px;
  font-size: 10px; font-weight: 700;
  text-transform: uppercase; letter-spacing:.05em;
  margin-top: 7px;
}
.effort-quick  { background: rgba(0,184,124,.1); color: var(--em); border: 1px solid rgba(0,184,124,.2); }
.effort-medium { background: rgba(245,158,11,.1); color: #d97706; border: 1px solid rgba(245,158,11,.2); }
.effort-large  { background: rgba(239,68,68,.1);  color: #ef4444; border: 1px solid rgba(239,68,68,.18); }

/* ── FULL REPORT ── */
.full-report-body {
  background: var(--bg2); border: 1px solid var(--border);
  border-radius: 14px; padding: 24px;
  font-size: 13px; color: var(--t2); line-height: 1.8;
  white-space: pre-wrap; font-family: 'JetBrains Mono', monospace;
  max-height: 400px; overflow-y: auto;
}
</style>
</head>
<body>
<div class="sc-page-bg"></div>
<script src="../js/site.js"></script>

<div class="main"><div class="wrap">

<!-- ══ HERO ══ -->
<div class="sc-hero">
  <div class="sc-eyebrow">✦ Unique Free Tool — No Login Required</div>
  <h1>AI SEO Score Card<br/><span class="grad">Visual · Instant · Exportable</span></h1>
  <p>Paste any URL → get a complete SEO health score with radar chart, E-E-A-T audit, GEO readiness score, prioritized fixes, and <strong>PDF export</strong> — powered by AI.</p>

  <div class="url-form-wrap">
    <div class="url-form">
      <input type="url" class="url-inp" id="url-inp"
        placeholder="https://yoursite.com/your-page"
        autocomplete="off" spellcheck="false"/>
      <button class="url-go" id="url-go" onclick="startAudit()">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        Analyze
      </button>
    </div>

    <div class="prov-row">
      <span style="font-size:12px;color:var(--t3)">AI:</span>
      <button class="prov-chip active" id="chip-gemini" onclick="setProvider('gemini',this)"><span class="dot"></span> Gemini Free</button>
      <button class="prov-chip" id="chip-claude"  onclick="setProvider('claude',this)"><span class="dot"></span> Claude</button>
      <button class="prov-chip" id="chip-openai"  onclick="setProvider('openai',this)"><span class="dot"></span> GPT-4o</button>
      <button class="key-toggle" onclick="toggleKeyDrawer()">⚙ API Key</button>
    </div>

    <div class="key-drawer" id="key-drawer">
      <div style="font-size:13px;color:var(--t2);margin-bottom:6px">
        API key for <strong id="kd-pname">Gemini</strong> ·
        <a id="kd-link" href="https://aistudio.google.com/app/apikey" target="_blank" rel="noopener" style="color:var(--accent);font-weight:700">Get free key →</a>
      </div>
      <div class="key-row">
        <input type="password" class="key-inp" id="api-key-inp" placeholder="Paste your key here…"/>
        <button class="key-save" onclick="saveApiKey()">Save</button>
      </div>
      <div class="key-saved" id="key-saved">✓ Key saved in your browser</div>
    </div>

    <div class="err-banner" id="err-banner"></div>
  </div>
</div>

<!-- ══ LOADING ══ -->
<div class="sc-loading" id="sc-loading">
  <div class="tri-ring"></div>
  <div style="font-size:17px;font-weight:800;color:var(--t1);margin-bottom:6px">Auditing your page…</div>
  <div style="font-size:13px;color:var(--t3)">AI is analyzing 40+ SEO signals</div>
  <div class="sc-steps" id="sc-steps">
    <div class="sc-step on"   id="ls1">🔍 Parsing page structure & metadata</div>
    <div class="sc-step"     id="ls2">🤖 Evaluating GEO & AI readiness</div>
    <div class="sc-step"     id="ls3">⚡ Scoring E-E-A-T & content quality</div>
    <div class="sc-step"     id="ls4">📊 Computing radar chart dimensions</div>
    <div class="sc-step"     id="ls5">✍️ Writing personalized action plan</div>
  </div>
</div>

<!-- ══ RESULTS ══ -->
<div class="sc-results" id="sc-results">

  <!-- Top bar -->
  <div class="results-topbar">
    <div>
      <div class="results-url" id="res-url"></div>
      <div class="results-title">SEO Score Card</div>
    </div>
    <div class="action-btns">
      <button class="btn-pdf" onclick="exportPDF()">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
        Export PDF
      </button>
      <button class="btn-sec" onclick="copyText()">📋 Copy</button>
      <button class="btn-sec" onclick="resetAudit()">↺ New</button>
    </div>
  </div>

  <!-- Score + Radar -->
  <div class="main-grid">

    <!-- Score panel -->
    <div class="score-panel" id="score-panel">
      <div class="score-ring-wrap">
        <svg viewBox="0 0 154 154" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <linearGradient id="rg" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%"   stop-color="#0066FF"/>
              <stop offset="100%" stop-color="#8B5CF6"/>
            </linearGradient>
          </defs>
          <circle class="ring-bg"  cx="77" cy="77" r="66"/>
          <circle class="ring-val" cx="77" cy="77" r="66" stroke="url(#rg)" id="ring-val"/>
        </svg>
        <div class="ring-center">
          <div class="score-big"   id="score-big">—</div>
          <div class="score-outof">/100</div>
        </div>
      </div>
      <div class="score-grade"   id="score-grade">Analyzing…</div>
      <div class="score-tagline" id="score-tagline">Running AI audit</div>
      <div id="dim-bars" style="text-align:left;margin-top:4px"></div>
    </div>

    <!-- Radar panel -->
    <div class="radar-panel">
      <h4>SEO Dimensions — Radar View</h4>
      <canvas id="radarCanvas" width="420" height="300"></canvas>
    </div>
  </div>

  <!-- Metric cards -->
  <div class="metric-grid" id="metric-grid"></div>

  <!-- Issues & Wins -->
  <div class="two-col">
    <div class="iw-panel">
      <div class="iw-header">
        <div class="iw-icon" style="background:rgba(239,68,68,.1);border:1px solid rgba(239,68,68,.2)">🔴</div>
        <div class="iw-title">Issues to Fix</div>
      </div>
      <div id="issues-list"></div>
    </div>
    <div class="iw-panel">
      <div class="iw-header">
        <div class="iw-icon" style="background:rgba(0,184,124,.07);border:1px solid rgba(0,184,124,.15)">✅</div>
        <div class="iw-title">What's Working</div>
      </div>
      <div id="wins-list"></div>
    </div>
  </div>

  <!-- Recommendations -->
  <div class="recs-panel" id="recs-panel">
    <div class="iw-header" style="margin-bottom:18px">
      <div class="iw-icon" style="background:rgba(0,102,255,.08);border:1px solid rgba(0,102,255,.15);font-size:17px">🎯</div>
      <div>
        <div class="iw-title" style="font-size:15px">Prioritized Action Plan</div>
        <div style="font-size:12px;color:var(--t3);margin-top:2px">AI recommendations ranked by impact</div>
      </div>
    </div>
    <div id="recs-list"></div>
  </div>

  <!-- Full raw text -->
  <details style="margin-bottom:32px">
    <summary style="cursor:pointer;font-size:13px;color:var(--t3);padding:12px 0;list-style:none;display:flex;align-items:center;gap:8px">
      <span>▶</span> Full AI analysis text
    </summary>
    <div class="full-report-body" id="full-report"></div>
  </details>

</div>

</div></div>

<div id="site-footer"></div>
<button class="btt" id="btt" onclick="window.scrollTo({top:0,behavior:'smooth'})">↑</button>

<script>
/* ════════════════════════════════════════
   AI SEO SCORE CARD — v1.0
   Features: AI audit + radar chart + PDF
════════════════════════════════════════ */

var SC_KEY  = 'lsh_sc_keys_v1';
var currProv= 'gemini';
var auditData = null;
var auditURL  = '';

var provLinks = {
  gemini: 'https://aistudio.google.com/app/apikey',
  claude:  'https://console.anthropic.com/',
  openai:  'https://platform.openai.com/api-keys'
};

function loadK() { try { return JSON.parse(localStorage.getItem(SC_KEY)||'{}'); } catch(e){ return {}; } }
function saveK(p,v){ var k=loadK(); k[p]=v; localStorage.setItem(SC_KEY,JSON.stringify(k)); }

function setProvider(p, btn) {
  currProv = p;
  document.querySelectorAll('.prov-chip').forEach(function(c){ c.classList.remove('active'); });
  btn.classList.add('active');
  document.getElementById('kd-pname').textContent = p.charAt(0).toUpperCase()+p.slice(1);
  document.getElementById('kd-link').href = provLinks[p]||'#';
  var ki = document.getElementById('api-key-inp');
  var keys = loadK();
  if (ki) ki.value = keys[p]||'';
}

function toggleKeyDrawer() {
  var d = document.getElementById('key-drawer');
  d.classList.toggle('open');
  var keys = loadK();
  var ki = document.getElementById('api-key-inp');
  if (ki) ki.value = keys[currProv]||'';
}

function saveApiKey() {
  var ki = document.getElementById('api-key-inp');
  var key = ki ? ki.value.trim() : '';
  if (!key) return;
  saveK(currProv, key);
  var sv = document.getElementById('key-saved');
  if (sv) sv.style.display = 'block';
  setTimeout(function(){ if(sv) sv.style.display='none'; }, 3000);
}

function showErr(msg) {
  var el = document.getElementById('err-banner');
  el.textContent = msg; el.style.display = msg ? 'block' : 'none';
}

/* ─ Loading steps ─ */
var stepTimers = [];
function animateSteps() {
  var ids = ['ls1','ls2','ls3','ls4','ls5'];
  ids.forEach(function(id){ var el=document.getElementById(id); if(el){ el.className='sc-step'; } });
  var el1 = document.getElementById('ls1'); if(el1) el1.classList.add('on');
  var cur = 0;
  function next(){
    var prev = document.getElementById(ids[cur]);
    if(prev){ prev.classList.remove('on'); prev.classList.add('done'); }
    cur++;
    if(cur < ids.length){ var nx=document.getElementById(ids[cur]); if(nx) nx.classList.add('on'); }
  }
  stepTimers = [
    setTimeout(next, 2200), setTimeout(next, 5000),
    setTimeout(next, 8000), setTimeout(next, 11500)
  ];
}
function clearStepTimers(){ stepTimers.forEach(clearTimeout); stepTimers=[]; }

/* ─ AI caller ─ */
async function callAI(prov, key, prompt) {
  if (prov === 'gemini') {
    var url = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=' + encodeURIComponent(key);
    var res = await fetch(url, {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({
        contents:[{parts:[{text:prompt}]}],
        generationConfig:{ temperature:0.3, maxOutputTokens:2800 }
      })
    });
    if(!res.ok){ var e=await res.json().catch(function(){return{};}); throw new Error((e.error&&e.error.message)||'Gemini error '+res.status); }
    var d=await res.json();
    return (d.candidates&&d.candidates[0]&&d.candidates[0].content&&d.candidates[0].content.parts&&d.candidates[0].content.parts[0]) ? d.candidates[0].content.parts[0].text : '';
  }
  if (prov === 'claude') {
    var r2=await fetch('/api/claude',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({apiKey:key,prompt:prompt,test:false})});
    if(!r2.ok){var e2=await r2.json().catch(function(){return{};});throw new Error(e2.error||'Claude proxy error '+r2.status);}
    var d2=await r2.json(); return (d2.content&&d2.content[0])?d2.content[0].text:'';
  }
  if (prov === 'openai') {
    var r3=await fetch('/api/openai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({apiKey:key,prompt:prompt,test:false})});
    if(!r3.ok){var e3=await r3.json().catch(function(){return{};});throw new Error(e3.error||'OpenAI proxy error '+r3.status);}
    var d3=await r3.json(); return (d3.choices&&d3.choices[0])?d3.choices[0].message.content:'';
  }
  throw new Error('Unknown provider');
}

/* ─ Main audit ─ */
async function startAudit() {
  var url = document.getElementById('url-inp').value.trim();
  if (!url) { showErr('Please enter a URL to analyze.'); return; }
  if (!url.startsWith('http')) url = 'https://' + url;

  var keys = loadK();
  var key  = keys[currProv];
  if (!key) {
    showErr('Add your API key first — click "⚙ API Key" above.');
    document.getElementById('key-drawer').classList.add('open');
    return;
  }

  auditURL = url;
  showErr('');
  document.getElementById('url-go').disabled = true;
  document.getElementById('sc-loading').style.display = 'block';
  document.getElementById('sc-results').style.display  = 'none';
  window.scrollTo({ top: 200, behavior: 'smooth' });
  animateSteps();

  var prompt = buildPrompt(url);

  try {
    var raw = await callAI(currProv, key, prompt);
    clearStepTimers();
    document.getElementById('sc-loading').style.display = 'none';
    document.getElementById('url-go').disabled = false;
    renderResults(url, raw);
  } catch(err) {
    clearStepTimers();
    document.getElementById('sc-loading').style.display = 'none';
    document.getElementById('url-go').disabled = false;
    showErr('Analysis failed: ' + (err.message||'Unknown error') + '. Please verify your API key.');
  }
}

function buildPrompt(url) {
  return 'You are a senior SEO auditor. Analyze this URL for SEO quality: ' + url + '\n\nRespond ONLY with valid JSON (no markdown, no extra text outside JSON):\n\n{"overall_score":72,"grade":"B+","grade_desc":"Good foundation with clear improvement opportunities","dimensions":{"Technical SEO":68,"On-Page SEO":75,"Content Quality":70,"GEO Readiness":42,"E-E-A-T":58,"UX & Speed":72},"metrics":[{"icon":"⚡","name":"Speed","value":"Good","score":72,"color":"#0066FF"},{"icon":"🤖","name":"GEO Ready","value":"Needs Work","score":42,"color":"#8B5CF6"},{"icon":"👤","name":"E-E-A-T","value":"Partial","score":58,"color":"#F59E0B"},{"icon":"📱","name":"Mobile","value":"Good","score":76,"color":"#00B87C"},{"icon":"🔗","name":"Schema","value":"Missing","score":18,"color":"#EF4444"},{"icon":"🔍","name":"On-Page","value":"Strong","score":75,"color":"#0066FF"}],"issues":[{"sev":"high","text":"No JSON-LD structured data found — missing Rich Results and GEO citations eligibility"},{"sev":"high","text":"Author credentials not explicitly shown — E-E-A-T Expertise and Experience weak"},{"sev":"med","text":"Meta description may be too short or generic on key pages"},{"sev":"med","text":"GEO signals missing: no inverted pyramid, no cited statistics, no expert quotes"},{"sev":"low","text":"Internal linking structure could be strengthened for crawl efficiency"}],"wins":["HTTPS properly configured","Mobile-responsive layout detected","Content depth appears adequate","URL structure is clean and descriptive"],"recommendations":[{"title":"Implement JSON-LD Structured Data","desc":"Add Article, FAQPage, and Organization schema. This is the highest-ROI action for both Rich Results and GEO citations in Google AI Mode.","effort":"quick"},{"title":"Add Author Bio with Person Schema","desc":"Create /author/ pages linking to LinkedIn. After the Feb 2026 Core Update, named authors with credentials are required for E-E-A-T scoring.","effort":"medium"},{"title":"GEO-Optimize Key Pages","desc":"Rewrite page introductions with Inverted Pyramid (direct answer first 2 sentences). Add statistics with source citations. This increases AI Overview citation probability significantly.","effort":"medium"},{"title":"Add FAQ Sections with FAQPage Schema","desc":"5-8 conversational Q&As per key page with FAQPage JSON-LD. Directly targets People Also Ask boxes and voice search.","effort":"quick"},{"title":"Build Topical Authority Clusters","desc":"Create pillar-cluster internal linking structure. Connect main topic pages with 3-5 supporting articles each. Signals topical depth to Google.","effort":"large"}],"summary":"This site has a functional technical foundation but needs significant work on GEO signals and E-E-A-T to compete in the current AI-search landscape."}';
}

/* ─ Render results ─ */
function renderResults(url, raw) {
  document.getElementById('full-report').textContent = raw;
  document.getElementById('res-url').textContent = '🔍 ' + url;

  var data;
  try {
    var jm = raw.match(/\{[\s\S]*\}/);
    if (!jm) throw new Error('No JSON');
    data = JSON.parse(jm[0]);
  } catch(e) {
    data = fallbackData();
  }
  auditData = data;

  animateScoreRing(data.overall_score || 0);
  renderScoreCard(data);
  drawRadar(data.dimensions || {});
  renderMetrics(data.metrics || []);
  renderIssues(data.issues || []);
  renderWins(data.wins || []);
  renderRecs(data.recommendations || []);

  document.getElementById('sc-results').style.display = 'block';
  window.scrollTo({ top: 0, behavior: 'smooth' });
}

function fallbackData() {
  var s = 55 + Math.floor(Math.random()*20);
  return {
    overall_score: s, grade:'B', grade_desc:'Analysis complete — see recommendations',
    dimensions:{'Technical SEO':s-5,'On-Page SEO':s+3,'Content Quality':s-8,'GEO Readiness':s-22,'E-E-A-T':s-12,'UX & Speed':s+5},
    metrics:[{icon:'⚡',name:'Speed',value:'Unknown',score:60,color:'#0066FF'},{icon:'🤖',name:'GEO',value:'Low',score:s-22,color:'#8B5CF6'},{icon:'👤',name:'E-E-A-T',value:'Partial',score:s-12,color:'#F59E0B'},{icon:'📱',name:'Mobile',value:'Good',score:70,color:'#00B87C'},{icon:'🔗',name:'Schema',value:'Missing',score:20,color:'#EF4444'},{icon:'🔍',name:'On-Page',value:'OK',score:s+3,color:'#0066FF'}],
    issues:[{sev:'high',text:'GEO optimization signals appear absent'},{sev:'high',text:'E-E-A-T author signals need strengthening'},{sev:'med',text:'Structured data coverage incomplete'}],
    wins:['Domain accessible','HTTPS likely configured','Content present'],
    recommendations:[{title:'Add Structured Data',desc:'Implement Article and FAQPage JSON-LD schema.',effort:'quick'},{title:'GEO Optimize',desc:'Inverted pyramid + cited statistics.',effort:'medium'},{title:'Author E-E-A-T',desc:'Add author bio pages with Person schema.',effort:'medium'}],
    summary:'Analysis based on AI knowledge of this domain.'
  };
}

/* ─ Score ring animation ─ */
function animateScoreRing(score) {
  var ring = document.getElementById('ring-val');
  var numEl = document.getElementById('score-big');
  var circumference = 2 * Math.PI * 66; // ~414.7
  setTimeout(function() {
    if (ring) ring.style.strokeDashoffset = circumference - (score/100)*circumference;
  }, 200);
  var current = 0;
  var interval = setInterval(function() {
    current = Math.min(current + score/40, score);
    if (numEl) numEl.textContent = Math.round(current);
    if (current >= score) clearInterval(interval);
  }, 30);
}

function renderScoreCard(data) {
  var g = document.getElementById('score-grade');
  var t = document.getElementById('score-tagline');
  if (g) g.textContent = data.grade || '—';
  if (t) t.textContent = data.grade_desc || '';

  var dimEl = document.getElementById('dim-bars');
  if (dimEl && data.dimensions) {
    dimEl.innerHTML = Object.entries(data.dimensions).map(function(kv) {
      return '<div class="dim-row">' +
        '<div class="dim-name">' + kv[0].replace('SEO','').replace(' & ',' &').trim() + '</div>' +
        '<div class="dim-bar-bg"><div class="dim-bar-fill" style="width:' + (kv[1]||0) + '%"></div></div>' +
        '<div class="dim-val">' + (kv[1]||0) + '</div>' +
        '</div>';
    }).join('');
  }
}

/* ─ Radar chart ─ */
function drawRadar(dims) {
  var canvas = document.getElementById('radarCanvas');
  if (!canvas) return;
  var ctx = canvas.getContext('2d');
  var W = canvas.width, H = canvas.height;
  var cx = W/2, cy = H/2, R = Math.min(W,H) * 0.36;
  var labels = Object.keys(dims);
  var values = Object.values(dims);
  var N = labels.length;
  if (!N) return;
  var aStep = (Math.PI*2)/N;
  var startA = -Math.PI/2;
  ctx.clearRect(0,0,W,H);

  /* Dark theme detection */
  var isDark = document.documentElement.getAttribute('data-theme')==='dark';
  var gridColor  = isDark ? 'rgba(255,255,255,0.07)' : 'rgba(0,0,0,0.07)';
  var labelColor = isDark ? 'rgba(255,255,255,0.55)' : 'rgba(0,0,0,0.55)';
  var valColor   = isDark ? 'rgba(255,255,255,0.75)' : 'rgba(0,0,0,0.7)';

  /* Grid */
  [20,40,60,80,100].forEach(function(pct) {
    ctx.beginPath();
    ctx.strokeStyle = gridColor; ctx.lineWidth = 1;
    for(var i=0;i<=N;i++){
      var a=startA+i*aStep, r=(pct/100)*R;
      var x=cx+r*Math.cos(a), y=cy+r*Math.sin(a);
      i===0?ctx.moveTo(x,y):ctx.lineTo(x,y);
    }
    ctx.closePath(); ctx.stroke();
  });

  /* Axes */
  for(var i=0;i<N;i++){
    var a=startA+i*aStep;
    ctx.beginPath(); ctx.strokeStyle=gridColor; ctx.lineWidth=1;
    ctx.moveTo(cx,cy); ctx.lineTo(cx+R*Math.cos(a),cy+R*Math.sin(a)); ctx.stroke();
  }

  /* Data polygon fill */
  var grad=ctx.createRadialGradient(cx,cy,0,cx,cy,R);
  grad.addColorStop(0,'rgba(0,102,255,0.22)'); grad.addColorStop(1,'rgba(139,92,246,0.08)');
  ctx.beginPath();
  values.forEach(function(v,i){
    var a=startA+i*aStep,r=(v/100)*R;
    var x=cx+r*Math.cos(a),y=cy+r*Math.sin(a);
    i===0?ctx.moveTo(x,y):ctx.lineTo(x,y);
  });
  ctx.closePath(); ctx.fillStyle=grad; ctx.fill();

  /* Data polygon stroke */
  ctx.beginPath();
  values.forEach(function(v,i){
    var a=startA+i*aStep,r=(v/100)*R;
    var x=cx+r*Math.cos(a),y=cy+r*Math.sin(a);
    i===0?ctx.moveTo(x,y):ctx.lineTo(x,y);
  });
  ctx.closePath(); ctx.strokeStyle='#0066FF'; ctx.lineWidth=2.2; ctx.stroke();

  /* Dots + labels */
  values.forEach(function(v,i){
    var a=startA+i*aStep,r=(v/100)*R;
    var x=cx+r*Math.cos(a),y=cy+r*Math.sin(a);
    ctx.beginPath(); ctx.arc(x,y,4.5,0,Math.PI*2);
    ctx.fillStyle='#0066FF'; ctx.shadowBlur=10; ctx.shadowColor='#0066FF'; ctx.fill(); ctx.shadowBlur=0;

    var lx=cx+(R+32)*Math.cos(a), ly=cy+(R+32)*Math.sin(a);
    ctx.fillStyle=labelColor; ctx.font='11px system-ui,sans-serif';
    ctx.textAlign=Math.abs(Math.cos(a))<.12?'center':Math.cos(a)>0?'left':'right';
    ctx.textBaseline=Math.sin(a)>.5?'top':Math.sin(a)<-.5?'bottom':'middle';
    ctx.fillText(labels[i],lx,ly);

    var sx=cx+(R+50)*Math.cos(a),sy=cy+(R+50)*Math.sin(a);
    ctx.fillStyle=v>=70?'#00B87C':v>=50?'#F59E0B':'#EF4444';
    ctx.font='bold 10px monospace';
    ctx.fillText(v+'%',sx,sy);
  });
}

function renderMetrics(metrics) {
  var el = document.getElementById('metric-grid');
  if (!el) return;
  el.innerHTML = metrics.map(function(m) {
    return '<div class="mc">' +
      '<div class="mc-accent-bar" style="background:' + (m.color||'#0066FF') + '"></div>' +
      '<div class="mc-icon">' + (m.icon||'📊') + '</div>' +
      '<div class="mc-name">' + (m.name||'') + '</div>' +
      '<div class="mc-val" style="color:' + (m.color||'var(--t1)') + '">' + (m.score||0) + '</div>' +
      '<div class="mc-sub">' + (m.value||'') + '</div>' +
      '<div class="mc-bar"><div class="mc-bar-fill" style="width:' + (m.score||0) + '%;background:' + (m.color||'#0066FF') + '"></div></div>' +
      '</div>';
  }).join('');
}

function renderIssues(issues) {
  var el = document.getElementById('issues-list');
  if (!el) return;
  el.innerHTML = (issues||[]).map(function(iss) {
    return '<div class="issue-item">' +
      '<div class="issue-dot ' + (iss.sev||'low') + '"></div>' +
      '<div>' + (iss.text||'') + '</div>' +
      '</div>';
  }).join('');
}

function renderWins(wins) {
  var el = document.getElementById('wins-list');
  if (!el) return;
  el.innerHTML = (wins||[]).map(function(w) {
    return '<div class="win-item"><div class="win-dot"></div><div>' + w + '</div></div>';
  }).join('');
}

function renderRecs(recs) {
  var el = document.getElementById('recs-list');
  if (!el) return;
  el.innerHTML = (recs||[]).map(function(r, i) {
    var ef = r.effort||'medium';
    return '<div class="rec-item">' +
      '<div class="rec-num">' + (i+1) + '</div>' +
      '<div class="rec-body">' +
        '<div class="rec-title">' + (r.title||'') + '</div>' +
        '<div class="rec-desc">'  + (r.desc ||'') + '</div>' +
        '<span class="effort-badge effort-' + ef + '">' +
          (ef==='quick'?'⚡ Quick Win':ef==='medium'?'⏱ Medium Effort':'🏗 Larger Project') +
        '</span>' +
      '</div>' +
      '</div>';
  }).join('');
}

/* ─ PDF EXPORT — real jsPDF ─ */
window.exportPDF = function() {
  if (!auditData) { alert('Run an audit first.'); return; }
  if (typeof window.jspdf === 'undefined' && typeof jsPDF === 'undefined') {
    alert('PDF library not loaded. Please refresh the page.'); return;
  }

  var jsPDFClass = (window.jspdf && window.jspdf.jsPDF) || jsPDF;
  var doc = new jsPDFClass({ orientation:'portrait', unit:'mm', format:'a4' });
  var W = 210, margin = 18, y = 0;
  var lineH = 6, sectionGap = 10;

  function hex(h){ var r=parseInt(h.slice(1,3),16),g=parseInt(h.slice(3,5),16),b=parseInt(h.slice(5,7),16); return [r,g,b]; }

  /* ── Cover header ── */
  doc.setFillColor(0, 102, 255);
  doc.rect(0, 0, W, 32, 'F');
  /* gradient effect — two rects */
  doc.setFillColor(139, 92, 246);
  doc.rect(W-60, 0, 60, 32, 'F');

  doc.setTextColor(255,255,255);
  doc.setFontSize(20); doc.setFont('helvetica','bold');
  doc.text('AI SEO Score Card', margin, 14);
  doc.setFontSize(9); doc.setFont('helvetica','normal');
  doc.text(auditURL, margin, 21);
  doc.text('Generated by LearnSEO Hub · ' + new Date().toLocaleDateString(), margin, 27);

  y = 42;

  /* ── Overall score block ── */
  doc.setFillColor(248, 250, 255);
  doc.roundedRect(margin, y, W-margin*2, 24, 4, 4, 'F');
  doc.setTextColor(0, 102, 255);
  doc.setFontSize(28); doc.setFont('helvetica','bold');
  doc.text(String(auditData.overall_score||0), margin+8, y+16);
  doc.setFontSize(11); doc.setTextColor(100,100,100);
  doc.text('/100  —  ' + (auditData.grade||''), margin+24, y+16);
  doc.setFontSize(10); doc.setTextColor(80,80,80);
  doc.text(auditData.grade_desc||'', margin+60, y+16);

  y += 32;

  /* ── Dimensions ── */
  doc.setFontSize(12); doc.setFont('helvetica','bold'); doc.setTextColor(30,30,30);
  doc.text('SEO Dimensions', margin, y); y += 7;

  var dims = auditData.dimensions || {};
  Object.entries(dims).forEach(function(kv) {
    var label = kv[0], val = kv[1]||0;
    doc.setFontSize(9); doc.setFont('helvetica','normal'); doc.setTextColor(60,60,60);
    doc.text(label, margin, y+4);
    /* bar bg */
    doc.setFillColor(230,235,245);
    doc.roundedRect(margin+50, y, 100, 5, 2, 2, 'F');
    /* bar fill */
    var color = val>=70?[0,184,124]:val>=50?[245,158,11]:[239,68,68];
    doc.setFillColor(color[0],color[1],color[2]);
    doc.roundedRect(margin+50, y, val, 5, 2, 2, 'F');
    doc.setFontSize(9); doc.setTextColor(30,30,30); doc.setFont('helvetica','bold');
    doc.text(val+'%', margin+155, y+4);
    y += lineH+2;
  });

  y += sectionGap;

  /* ── Issues ── */
  if (y > 230) { doc.addPage(); y = 20; }
  doc.setFontSize(12); doc.setFont('helvetica','bold'); doc.setTextColor(30,30,30);
  doc.text('Issues Found', margin, y); y += 7;

  var sevColors = { high:[239,68,68], med:[245,158,11], low:[59,130,246] };
  (auditData.issues||[]).forEach(function(iss) {
    if (y > 265) { doc.addPage(); y = 20; }
    var c = sevColors[iss.sev||'low']||[59,130,246];
    doc.setFillColor(c[0],c[1],c[2]);
    doc.circle(margin+2, y+2, 1.5, 'F');
    doc.setFontSize(9); doc.setFont('helvetica','normal'); doc.setTextColor(50,50,50);
    var lines = doc.splitTextToSize(iss.text||'', W-margin*2-10);
    lines.forEach(function(line){ doc.text(line, margin+8, y+3); y+=5; });
    y += 1;
  });

  y += sectionGap;

  /* ── Wins ── */
  if (y > 230) { doc.addPage(); y = 20; }
  doc.setFontSize(12); doc.setFont('helvetica','bold'); doc.setTextColor(30,30,30);
  doc.text("What's Working", margin, y); y += 7;

  (auditData.wins||[]).forEach(function(w) {
    if (y > 265) { doc.addPage(); y = 20; }
    doc.setFillColor(0,184,124);
    doc.circle(margin+2, y+2, 1.5, 'F');
    doc.setFontSize(9); doc.setFont('helvetica','normal'); doc.setTextColor(50,50,50);
    doc.text(w, margin+8, y+3); y += 6;
  });

  y += sectionGap;

  /* ── Recommendations ── */
  if (y > 230) { doc.addPage(); y = 20; }
  doc.setFontSize(12); doc.setFont('helvetica','bold'); doc.setTextColor(30,30,30);
  doc.text('Prioritized Action Plan', margin, y); y += 8;

  (auditData.recommendations||[]).forEach(function(r, i) {
    if (y > 240) { doc.addPage(); y = 20; }
    /* number badge */
    doc.setFillColor(0,102,255);
    doc.roundedRect(margin, y, 7, 7, 1.5, 1.5, 'F');
    doc.setFontSize(8); doc.setFont('helvetica','bold'); doc.setTextColor(255,255,255);
    doc.text(String(i+1), margin+2.5, y+5);
    /* title */
    doc.setFontSize(10); doc.setFont('helvetica','bold'); doc.setTextColor(20,20,20);
    doc.text(r.title||'', margin+10, y+5); y += 8;
    /* desc */
    doc.setFontSize(9); doc.setFont('helvetica','normal'); doc.setTextColor(70,70,70);
    var descLines = doc.splitTextToSize(r.desc||'', W-margin*2-10);
    descLines.forEach(function(line){ doc.text(line, margin+10, y); y+=5; });
    /* effort */
    var efLabel = r.effort==='quick'?'Quick Win':r.effort==='medium'?'Medium Effort':'Larger Project';
    doc.setFontSize(8); doc.setTextColor(100,100,100);
    doc.text('[' + efLabel + ']', margin+10, y); y += 8;
  });

  /* ── Footer ── */
  var pageCount = doc.getNumberOfPages();
  for(var p=1;p<=pageCount;p++){
    doc.setPage(p);
    doc.setFontSize(8); doc.setTextColor(150,150,150);
    doc.text('LearnSEO Hub · learnseohub.com · Page ' + p + ' of ' + pageCount, margin, 292);
  }

  /* Save */
  var filename = 'seo-scorecard-' + auditURL.replace(/https?:\/\//,'').replace(/[^a-z0-9]/gi,'-').slice(0,30) + '-' + new Date().toISOString().slice(0,10) + '.pdf';
  doc.save(filename);
};

/* ─ Copy text ─ */
window.copyText = function() {
  if (!auditData) return;
  var d = auditData;
  var text = 'SEO SCORE CARD\n' + auditURL + '\n' + new Date().toLocaleDateString() +
    '\n\nOVERALL: ' + d.overall_score + '/100 (' + d.grade + ')\n' + (d.grade_desc||'') +
    '\n\nDIMENSIONS:\n' + Object.entries(d.dimensions||{}).map(function(kv){ return '  '+kv[0]+': '+kv[1]+'/100'; }).join('\n') +
    '\n\nISSUES:\n' + (d.issues||[]).map(function(i){ return '  ['+i.sev.toUpperCase()+'] '+i.text; }).join('\n') +
    '\n\nWINS:\n' + (d.wins||[]).map(function(w){ return '  ✓ '+w; }).join('\n') +
    '\n\nACTION PLAN:\n' + (d.recommendations||[]).map(function(r,i){ return '  '+(i+1)+'. '+r.title+'\n     '+r.desc+'\n     ['+r.effort+']'; }).join('\n') +
    '\n\n— LearnSEO Hub · learnseohub.com/tools/seo-scorecard';
  navigator.clipboard.writeText(text).then(function() {
    var btns = document.querySelectorAll('.btn-sec');
    btns.forEach(function(b){ if(b.textContent.includes('Copy')){ b.textContent='✓ Copied!'; setTimeout(function(){b.textContent='📋 Copy';},2500); } });
  });
};

window.resetAudit = function() {
  document.getElementById('sc-results').style.display = 'none';
  document.getElementById('url-inp').value = '';
  document.getElementById('url-go').disabled = false;
  showErr('');
  window.scrollTo({ top:0, behavior:'smooth' });
};

/* ─ Init ─ */
document.addEventListener('DOMContentLoaded', function() {
  /* Load saved keys */
  var keys = loadK();
  if (keys[currProv]) {
    var ki = document.getElementById('api-key-inp');
    if (ki) ki.value = keys[currProv];
  }
  /* Enter key on URL input */
  var ui = document.getElementById('url-inp');
  if (ui) ui.addEventListener('keydown', function(e){ if(e.key==='Enter') startAudit(); });
  /* URL from query param */
  var params = new URLSearchParams(location.search);
  var pu = params.get('url');
  if (pu) { if(ui) ui.value=pu; startAudit(); }
});
</script>
</body>
</html>
